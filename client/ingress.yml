apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: go-grpc-headless-service-client-ingress
spec:
  ingressClassName: public
  rules:
  - host: go-grpc-headless-service-client.demo
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: go-grpc-headless-service-client
            port:
              number: 80