apiVersion: apps/v1
kind: Deployment
metadata:
  name: grcp-xdsserver
  labels:
    app.kubernetes.io/name: grcp-xdsserver
    name: grcp-xdsserver
spec:
  replicas: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: grcp-xdsserver
  template:
    metadata:
      labels:
        app.kubernetes.io/name: grcp-xdsserver
    spec:
      restartPolicy: Always
      containers:
      - name: grcp-xdsserver
        image: ${ARTIFACT_REGION}-docker.pkg.dev/${ARTIFACT_PROJECT_ID}/grcp-xdsserver/dev:latest
        imagePullPolicy: Always
        ports:
          - name: tcp
            containerPort: 9090
        livenessProbe:
         grpc:
           port: 9090
         initialDelaySeconds: 5
         periodSeconds: 10

---

apiVersion: v1
kind: Service
metadata:
  name: grcp-xdsserver
  labels:
    app.kubernetes.io/name: grcp-xdsserver
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: grcp-xdsserver
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 9090
  - name: https
    port: 443
    protocol: TCP
    targetPort: 9090